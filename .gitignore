# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/

# Virtualenvs
.venv/
venv/
env/

# Local config / secrets
.env
.env.local

# App-local state — recreated on first run
screens.db
*.db-journal
*.db-shm
*.db-wal

# Plan-execution artifacts (logs + screenshots) — per-developer, large, regenerable
runs/

# User-uploaded Excel files for the bulk-load workflow.
uploads/

# Playwright MCP server's session cache (page snapshots, console logs).
# Recreated automatically every run.
.playwright-mcp/

# Smoke-test artifacts (Playwright + temp scripts)
_pw_*.png
_pw_*.py
_smoke.py

# IDE / editor
.vscode/
.idea/
*.swp
*~

# Claude Code — keep shared project config (settings.json) but ignore
# per-developer overrides and session-accumulated permission caches.
.claude/settings.local.json
.claude/cache/

# OS
.DS_Store
Thumbs.db

# Build / dist
build/
dist/
